meta {
  name: Me
  type: http
  seq: 1
}

get {
  url: {{api_url}}/tenant/auth/me
  body: none
  auth: inherit
}

headers {
  Accept: application/json
  X-Tenant-ID: {{tenant_id}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
